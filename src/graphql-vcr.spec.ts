import graphqlVcr from './graphql-vcr'

describe('graphqlVcr', function() {
  it('can record', () => {
    expect(true).toBe(true)
  })

  it('can replay', () => {
    expect(true).toBe(true)
  })
})
